import {useEffect,useState} from 'react';import {Navigation} from '../components/Navigation';import {apiDelete,apiGet} from '../lib/api';import {Card,CardContent} from '../components/ui/card';import {Button} from '../components/ui/button';import {Badge} from '../components/ui/badge';import {Link} from 'react-router';import {BookmarkX} from 'lucide-react';import {toast} from 'sonner';
export default function SavedJobs(){const[jobs,setJobs]=useState<any[]>([]);const load=()=>apiGet<any>('/saved-jobs').then(d=>setJobs(d.jobs)).catch((e:any)=>toast.error(e.message));useEffect(load,[]);async function remove(id:string){await apiDelete(`/saved-jobs/${id}`);setJobs(x=>x.filter(j=>j.id!==id));}return <div className="min-h-screen bg-slate-950 text-white"><Navigation/><main className="max-w-5xl mx-auto px-6 pt-28 pb-16"><h1 className="text-4xl font-bold">Saved opportunities</h1><p className="text-slate-400 mt-2 mb-7">A focused shortlist of roles you want to revisit.</p>{jobs.length===0?<Card className="bg-white/5 border-white/10"><CardContent className="p-12 text-center"><p className="text-slate-400">You have not saved any opportunities yet.</p><Link to="/jobseeker/jobs"><Button className="mt-4">Browse opportunities</Button></Link></CardContent></Card>:<div className="space-y-4">{jobs.map(j=><Card key={j.id} className="bg-white/[.055] border-white/10"><CardContent className="p-5 flex justify-between gap-4"><Link to={`/jobseeker/jobs/${j.id}`}><Badge variant="secondary">{j.opportunity_kind||'job'}</Badge><h2 className="font-semibold text-xl mt-2">{j.title}</h2><p className="text-violet-300">{j.company}</p><p className="text-sm text-slate-400 mt-2">{j.location} · {j.workplace}</p></Link><Button variant="ghost" size="icon" onClick={()=>remove(j.id)}><BookmarkX/></Button></CardContent></Card>)}</div>}</main></div>}
