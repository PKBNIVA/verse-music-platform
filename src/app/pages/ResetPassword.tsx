import {useState} from 'react';import {Link,useSearchParams} from 'react-router';import {PublicNav} from '../components/PublicNav';import {Input} from '../components/ui/input';import {Button} from '../components/ui/button';import {apiPost} from '../lib/api';import {toast} from 'sonner';
export default function ResetPassword(){const[sp]=useSearchParams(),[password,setPassword]=useState(''),[done,setDone]=useState(false);async function submit(){try{await apiPost('/auth/reset-password',{token:sp.get('token'),password});setDone(true)}catch(e:any){toast.error(e.message)}}return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-md mx-auto px-5 py-24"><h1 className="text-4xl font-bold">Choose a new password</h1>{done?<Button className="mt-6" asChild><Link to="/auth/jobseeker">Sign in</Link></Button>:<div className="mt-6 space-y-3"><Input value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="At least 10 characters" className="bg-white/5 border-white/15"/><Button className="w-full" onClick={submit}>Update password</Button></div>}</main></div>}
