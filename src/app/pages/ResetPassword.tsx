import {useState} from 'react';import {Link,useSearchParams} from 'react-router';import {PublicNav} from '../components/PublicNav';import {Input} from '../components/ui/input';import {Label} from '../components/ui/label';import {Button} from '../components/ui/button';import {apiPost} from '../lib/api';import {toast} from 'sonner';
export default function ResetPassword(){const[sp]=useSearchParams(),[password,setPassword]=useState(''),[done,setDone]=useState(false),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);try{await apiPost('/auth/reset-password',{token:sp.get('token'),password});setDone(true)}catch(e:any){toast.error(e.message)}finally{setBusy(false)}}return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-md mx-auto px-5 py-24"><h1 className="text-4xl font-bold">Choose a new password</h1>{done?<><p className="text-slate-400 mt-3" role="status">Your password has been updated.</p><Button className="mt-6" asChild><Link to="/auth/jobseeker">Sign in</Link></Button></>:<form onSubmit={submit} className="mt-6 space-y-3" aria-busy={busy}><Label htmlFor="reset-password" className="text-slate-200">New password</Label><Input id="reset-password" name="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} type="password" required minLength={10} placeholder="At least 10 characters" aria-describedby="reset-password-help" className="bg-white/5 border-white/15"/><p id="reset-password-help" className="text-xs text-slate-400">Use at least 10 characters.</p><Button type="submit" className="w-full" disabled={busy}>{busy?'Updating…':'Update password'}</Button></form>}</main></div>}
