import {useState} from 'react';import {PublicNav} from '../components/PublicNav';import {Input} from '../components/ui/input';import {Label} from '../components/ui/label';import {Button} from '../components/ui/button';import {apiPost} from '../lib/api';import {toast} from 'sonner';
export default function ForgotPassword(){const[email,setEmail]=useState(''),[done,setDone]=useState(false),[busy,setBusy]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);try{const d:any=await apiPost('/auth/forgot-password',{email});setDone(true);if(d.debugLink)toast.message(`Dev reset link: ${d.debugLink}`)}catch(e:any){toast.error(e.message)}finally{setBusy(false)}}return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-md mx-auto px-5 py-24"><h1 className="text-4xl font-bold">Reset password</h1><p className="text-slate-400 mt-3" aria-live="polite">{done?'If an account exists, reset instructions have been sent.':'Enter your account email.'}</p>{!done&&<form onSubmit={submit} className="mt-6 space-y-3" aria-busy={busy}><Label htmlFor="forgot-email" className="text-slate-200">Email</Label><Input id="forgot-email" name="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} type="email" required className="bg-white/5 border-white/15"/><Button type="submit" className="w-full" disabled={busy}>{busy?'Sending…':'Send reset link'}</Button></form>}</main></div>}
