import {useState} from 'react';import {PublicNav} from '../components/PublicNav';import {Input} from '../components/ui/input';import {Button} from '../components/ui/button';import {apiPost} from '../lib/api';import {toast} from 'sonner';
export default function ForgotPassword(){const[email,setEmail]=useState(''),[done,setDone]=useState(false);async function submit(){try{const d:any=await apiPost('/auth/forgot-password',{email});setDone(true);if(d.debugLink)toast.message(`Dev reset link: ${d.debugLink}`)}catch(e:any){toast.error(e.message)}}return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-md mx-auto px-5 py-24"><h1 className="text-4xl font-bold">Reset password</h1><p className="text-slate-400 mt-3">{done?'If an account exists, reset instructions have been sent.':'Enter your account email.'}</p>{!done&&<div className="mt-6 space-y-3"><Input value={email} onChange={e=>setEmail(e.target.value)} type="email" className="bg-white/5 border-white/15"/><Button className="w-full" onClick={submit}>Send reset link</Button></div>}</main></div>}
