import {useEffect,useState} from 'react';import {Navigation} from '../components/Navigation';import {apiGet,apiPost} from '../lib/api';import {Card,CardContent} from '../components/ui/card';import {Button} from '../components/ui/button';import {Badge} from '../components/ui/badge';import {Check,ShieldCheck} from 'lucide-react';import {toast} from 'sonner';
async function openRazorpay(opts:any,onSuccess:()=>void){await new Promise<void>((resolve,reject)=>{if((window as any).Razorpay)return resolve();const sc=document.createElement('script');sc.src='https://checkout.razorpay.com/v1/checkout.js';sc.onload=()=>resolve();sc.onerror=()=>reject(new Error('Unable to load Razorpay Checkout'));document.body.appendChild(sc)});const rz=new (window as any).Razorpay({key:opts.keyId,subscription_id:opts.subscriptionId,name:'Verse',description:'Verse SaaS subscription',handler:()=>onSuccess(),theme:{}});rz.open()}
export default function Billing(){const[plans,setPlans]=useState<any[]>([]),[sub,setSub]=useState<any>(null);const load=()=>Promise.all([apiGet<any>('/billing/plans'),apiGet<any>('/billing/subscription')]).then(([p,s])=>{setPlans(p.plans||[]);setSub(s)});useEffect(()=>{load()},[]);async function choose(code:string){try{const d:any=await apiPost('/billing/checkout',{planCode:code});if(d.salesAssisted){toast.info(d.message);return}if(d.checkout?.mode==='razorpay'){await openRazorpay(d.checkout,()=>{toast.success('Mandate authorised. Paid access will activate after the signed billing webhook is processed.');setTimeout(load,1200)})}else if(d.checkout?.mode==='mock'){toast.success('Trial activated in development mode. Configure Razorpay environment keys for live billing.');load()}}catch(e:any){toast.error(e.message)}}return <div className="min-h-screen bg-slate-950 text-white"><Navigation/><main className="max-w-7xl mx-auto px-5 pt-28 pb-16"><div className="flex flex-col md:flex-row md:items-end justify-between gap-4"><div><div className="text-xs uppercase tracking-[.2em] text-violet-300">SaaS billing</div><h1 className="text-4xl font-bold mt-2">Plan & billing</h1><p className="text-slate-400 mt-2">Core candidate applications remain free. Paid plans unlock operating capacity for hiring, sourcing, booking and teams.</p></div>{sub&&<Badge className="w-fit">{sub.subscription?.status==='pending'?sub.purchasedPlan?.name:sub.plan?.name} · {sub.subscription?.status}</Badge>}</div>{sub?.subscription?.status==='trialing'&&sub?.subscription?.trial_ends_at&&<Card className="mt-6 bg-emerald-500/[.06] border-emerald-400/20"><CardContent className="p-5 flex gap-3"><ShieldCheck className="text-emerald-300"/><div><b>Free trial active</b><div className="text-sm text-slate-400">Ends {new Date(sub.subscription.trial_ends_at).toLocaleDateString()}. Billing starts only after the trial if a valid recurring mandate is active.</div></div></CardContent></Card>}<div className="grid md:grid-cols-2 xl:grid-cols-4 gap-4 mt-7">{plans.map(p=><Card key={p.code} className={`bg-white/[.055] border-white/10 ${sub?.subscription?.plan_code===p.code?'ring-1 ring-violet-400':''}`}><CardContent className="p-5"><h2 className="text-xl font-semibold">{p.name}</h2><div className="text-3xl font-bold mt-3">{p.monthly===null?'Custom':p.monthly===0?'Free':`₹${Number(p.monthly).toLocaleString('en-IN')}`} {p.monthly>0&&<span className="text-xs font-normal text-slate-500">/month</span>}</div>{p.trialDays>0&&<div className="text-sm text-emerald-300 mt-1">{p.trialDays}-day free trial</div>}<div className="space-y-2 text-sm text-slate-300 mt-5">{[`Up to ${p.activePosts} active opportunities`,`${p.seats} team seat${p.seats===1?'':'s'}`,`${p.shortlist} saved talent capacity`,`${p.bookings} active booking enquiries`].map(x=><div className="flex gap-2" key={x}><Check size={15} className="text-emerald-300 mt-0.5"/>{x}</div>)}</div>{p.code!=='free'&&<Button className="w-full mt-6" variant={sub?.subscription?.plan_code===p.code?'secondary':'default'} disabled={['active','trialing'].includes(sub?.subscription?.status)&&sub?.subscription?.plan_code===p.code} onClick={()=>choose(p.code)}>{sub?.subscription?.plan_code===p.code&&sub?.subscription?.status==='pending'?'Complete setup':sub?.subscription?.plan_code===p.code&&['active','trialing'].includes(sub?.subscription?.status)?'Current plan':p.code==='enterprise'?'Contact sales':'Start free trial'}</Button>}</CardContent></Card>)}</div><div className="mt-8 text-xs text-slate-500 max-w-4xl">Payment state is webhook-driven; client redirects never grant entitlements. Live Razorpay activation requires server-side keys, per-plan Razorpay plan IDs and a webhook secret. Trial, cancellation and plan limits are stored server-side.</div></main></div>}
