import {useEffect,useState} from 'react';import {Link} from 'react-router';import {PublicNav} from '../../components/PublicNav';import {Card,CardContent} from '../../components/ui/card';import {Badge} from '../../components/ui/badge';import {apiGet} from '../../lib/api';import {MapPin,ShieldCheck} from 'lucide-react';
export default function PublicActs(){const[acts,setActs]=useState<any[]>([]);useEffect(()=>{apiGet<any>('/public/acts').then(d=>setActs(d.acts||[]))},[]);return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-6xl mx-auto px-5 py-14"><p className="text-xs uppercase tracking-[.22em] text-violet-300">Live music booking</p><h1 className="text-4xl md:text-6xl font-bold mt-2">Book singers, bands, ensembles & live acts</h1><p className="text-slate-400 mt-4">Discover public acts, inspect lineup and repertoire, then sign in to request availability and a quote.</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">{acts.map(a=><Link key={a.id} to={`/acts/${a.id}`}><Card className="h-full bg-white/[.05] border-white/10 hover:bg-white/[.075]"><CardContent className="p-5"><div className="flex items-center gap-2"><h2 className="text-xl font-semibold">{a.name}</h2>{a.verified&&<ShieldCheck size={16} className="text-emerald-300"/>}</div><p className="text-violet-300 mt-1">{a.act_type}</p>{a.city&&<p className="text-sm text-slate-400 mt-3 flex"><MapPin size={15} className="mr-1"/>{a.city}</p>}<p className="text-sm text-slate-300 mt-3 line-clamp-3">{a.tagline||a.bio}</p><div className="flex flex-wrap gap-2 mt-4">{a.genres?.slice(0,4).map((x:string)=><Badge key={x} variant="secondary">{x}</Badge>)}</div></CardContent></Card></Link>)}</div></main></div>}
