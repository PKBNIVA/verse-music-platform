import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router';import {PublicNav} from '../../components/PublicNav';import {Card,CardContent} from '../../components/ui/card';import {Badge} from '../../components/ui/badge';import {Button} from '../../components/ui/button';import {apiGet} from '../../lib/api';import {MapPin,ShieldCheck,Wallet,CalendarDays} from 'lucide-react';
export default function PublicOpportunity(){const{id}=useParams();const[j,setJ]=useState<any>();useEffect(()=>{apiGet<any>(`/jobs/${id}`).then(d=>setJ(d.job))},[id]);if(!j)return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><div className="p-20 text-center text-slate-400">Loading opportunity…</div></div>;return <div className="min-h-screen bg-slate-950 text-white"><PublicNav/><main className="max-w-5xl mx-auto px-5 py-12"><Card className="bg-white/[.055] border-white/10"><CardContent className="p-7 md:p-10"><div className="flex flex-wrap gap-2"><Badge>{j.opportunity_kind}</Badge>{j.employerVerified&&<Badge className="bg-emerald-500/10 text-emerald-300"><ShieldCheck size={13} className="mr-1"/>Verified hiring party</Badge>}</div><h1 className="text-4xl md:text-6xl font-bold mt-4">{j.title}</h1><p className="text-xl text-violet-300 mt-2">{j.company}</p><div className="grid sm:grid-cols-3 gap-3 mt-7 text-sm text-slate-300"><span className="flex gap-2"><MapPin size={17}/>{j.location}</span><span className="flex gap-2"><Wallet size={17}/>{j.salary||`${j.currency||'INR'} ${j.compensation_min||''}${j.compensation_max?`–${j.compensation_max}`:''}`}</span><span className="flex gap-2"><CalendarDays size={17}/>{j.application_deadline?`Apply by ${j.application_deadline}`:'Open until filled'}</span></div><div className="mt-8 pt-7 border-t border-white/10"><h2 className="text-xl font-semibold">About the opportunity</h2><p className="whitespace-pre-wrap leading-7 text-slate-300 mt-3">{j.description}</p>{j.requirements&&<><h2 className="text-xl font-semibold mt-8">Requirements</h2><p className="whitespace-pre-wrap leading-7 text-slate-300 mt-3">{j.requirements}</p></>}</div><div className="flex flex-wrap gap-2 mt-7">{j.skills?.map((x:string)=><Badge key={x} variant="outline">{x}</Badge>)}</div><Button size="lg" className="mt-8" asChild><Link to="/auth/jobseeker">Sign in to apply</Link></Button></CardContent></Card><p className="text-xs text-slate-500 mt-5">Never pay private application or audition fees. Verse listings can be reported after sign-in.</p></main></div>}
