import {useEffect,useState} from 'react';import {Navigation} from '../components/Navigation';import {Card,CardContent} from '../components/ui/card';import {Badge} from '../components/ui/badge';import {Button} from '../components/ui/button';import {Label} from '../components/ui/label';import {Textarea} from '../components/ui/textarea';import {apiGet,apiPatch,apiPost} from '../lib/api';import {toast} from 'sonner';import {Link,useNavigate,useSearchParams} from 'react-router';import {useAuth} from '../lib/authContext';import {FormDialog,fieldClass} from '../components/HiringDialog';
// datetime-local value for "now", in the viewer's time zone.
const localNow=()=>{const d=new Date();d.setSeconds(0,0);return new Date(d.getTime()-d.getTimezoneOffset()*60000).toISOString().slice(0,16)};
export default function EmployerApplications(){const nav=useNavigate(),{user}=useAuth(),[params,setParams]=useSearchParams();const base=user?.role==='jobseeker'?'/jobseeker':'/employer',postPath=user?.role==='jobseeker'?'/jobseeker/hiring/post':'/employer/post-job';const jobId=params.get('jobId')||'';const [apps,setApps]=useState<any[]>([]),[jobs,setJobs]=useState<any[]>([]),[loading,setLoading]=useState(true),[loadError,setLoadError]=useState('');/* Applications with a status/note update in flight; their actions are disabled until it settles. */const [updating,setUpdating]=useState<Record<string,boolean>>({});const markUpdating=(id:string,on:boolean)=>setUpdating(xs=>{const next={...xs};if(on)next[id]=true;else delete next[id];return next});
const [interview,setInterview]=useState<{id:string;name:string;date:string}|null>(null),[notes,setNotes]=useState<{id:string;name:string;note:string;rating:string}|null>(null);
const load=()=>apiGet<any>(`/employer/applications?${new URLSearchParams(jobId?{jobId}:{})}`).then(d=>{setApps(d.applications||[]);setLoadError('')}).catch((e:any)=>setLoadError(e.message||'Applications could not be loaded.')).finally(()=>setLoading(false));
useEffect(()=>{setLoading(true);load()},[jobId]);useEffect(()=>{apiGet<any>('/employer/jobs').then(d=>setJobs(d.jobs||[])).catch(()=>{})},[]);
async function message(candidateId:string,jobId:string){try{const d=await apiPost<any>('/conversations',{candidateId,jobId});nav(`${base}/messages?conversation=${d.conversation.id}`)}catch(e:any){toast.error(e.message)}}
async function update(id:string,body:any,success:string){if(updating[id])return false;markUpdating(id,true);try{await apiPatch(`/employer/applications/${id}`,body);toast.success(success);await load();return true}catch(e:any){toast.error(e.message);return false}finally{markUpdating(id,false)}}
function status(a:any,s:string){if(s==='Interview Scheduled'){setInterview({id:a.id,name:a.candidateName,date:''});return}void update(a.id,{status:s},`Marked ${s}`)}
async function saveInterview(){if(!interview)return;const parsed=new Date(interview.date);if(!interview.date||Number.isNaN(parsed.getTime())){toast.error('Choose a valid interview date and time');return}if(await update(interview.id,{status:'Interview Scheduled',interviewDate:parsed.toISOString()},'Marked Interview Scheduled'))setInterview(null)}
async function saveNotes(){if(!notes)return;if(await update(notes.id,{recruiterNote:notes.note,recruiterRating:notes.rating?Number(notes.rating):null},'Recruiter notes saved'))setNotes(null)}
const setJobFilter=(v:string)=>setParams(v?{jobId:v}:{},{replace:true});const filteredJob=jobs.find(j=>j.id===jobId);
return <div className="min-h-screen bg-slate-950 text-white"><Navigation/><main className="max-w-6xl mx-auto px-5 md:px-6 pt-28 pb-16"><h1 className="text-4xl font-bold">Applications</h1><p className="text-slate-400 mt-2 mb-5">Review candidates only for opportunities you posted.</p>
{jobs.length>0&&<div className="mb-6 max-w-md"><Label htmlFor="application-job-filter">Opportunity</Label><select id="application-job-filter" value={jobId} onChange={e=>setJobFilter(e.target.value)} className="mt-2 w-full h-10 rounded-md bg-slate-900 border border-white/15 px-3"><option value="">All opportunities</option>{jobs.map(j=><option key={j.id} value={j.id}>{j.title} ({j.status})</option>)}</select></div>}
{loading?<Card className="bg-white/5 border-white/10"><CardContent className="p-10 text-center text-slate-400" role="status">Loading applications…</CardContent></Card>:loadError?<Card className="bg-white/5 border-white/10"><CardContent className="p-10 text-center" role="alert"><p className="text-rose-300">{loadError}</p><Button className="mt-4" variant="outline" onClick={()=>{setLoading(true);load()}}>Try again</Button></CardContent></Card>:apps.length===0?<Card className="bg-white/5 border-white/10"><CardContent className="p-10 text-center text-slate-400"><p>{jobId?`No applications for ${filteredJob?.title||'this opportunity'} yet.`:'No applications yet.'}</p>{jobId?<Button className="mt-4" variant="outline" onClick={()=>setJobFilter('')}>Show all opportunities</Button>:<Button className="mt-4" variant="outline" asChild><Link to={postPath}>Create an opportunity</Link></Button>}</CardContent></Card>:<div className="space-y-4">{apps.map(a=><Card key={a.id} className="bg-white/[.06] border-white/10"><CardContent className="p-6"><div className="flex flex-col lg:flex-row gap-5 justify-between"><div className="flex-1 min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-semibold">{a.candidateName}</h2><Badge>{a.status}</Badge></div><div className="text-violet-300 text-sm mt-1">Applied for {a.jobTitle}</div>{a.status==='Interview Scheduled'&&a.interviewDate&&<div className="text-sm text-emerald-300 mt-1">Interview: {new Date(a.interviewDate).toLocaleString()}</div>}<div className="text-sm text-slate-400 mt-2 break-words">{a.candidateEmail} · {a.candidateLocation||'Location not provided'} · {a.experience||'Experience not provided'}</div><div className="flex flex-wrap gap-2 mt-3">{a.skills?.map((s:string)=><Badge variant="secondary" key={s}>{s}</Badge>)}</div>{a.coverLetter&&<p className="text-sm text-slate-300 mt-4 border-l-2 border-violet-500 pl-3 whitespace-pre-line break-words">{a.coverLetter}</p>}{a.screeningAnswers?.length>0&&<div className="mt-4 text-sm space-y-2"><div className="text-slate-500">Screening responses</div>{a.screeningAnswers.map((x:any,i:number)=><div key={i} className="p-2 rounded bg-white/5 break-words">{typeof x==='string'?x:JSON.stringify(x)}</div>)}</div>}{(a.recruiterNote||a.recruiterRating)&&<div className="text-xs text-amber-200 mt-3">Internal: {a.recruiterRating?`${a.recruiterRating}/5 · `:''}{a.recruiterNote}</div>}</div><div className="flex flex-wrap lg:flex-col gap-2 lg:w-52"><Button size="sm" onClick={()=>message(a.candidateId,a.jobId)}>Message</Button><Button size="sm" variant="outline" disabled={!!updating[a.id]} onClick={()=>setNotes({id:a.id,name:a.candidateName,note:a.recruiterNote||'',rating:a.recruiterRating?String(a.recruiterRating):''})}>Rate / note</Button>{(a.allowedNextStatuses||[]).map((s:string)=><Button key={s} size="sm" variant={s==='Rejected'?'outline':'secondary'} disabled={!!updating[a.id]} aria-busy={!!updating[a.id]} onClick={()=>status(a,s)}>{s}</Button>)}</div></div></CardContent></Card>)}</div>}
<FormDialog open={!!interview} onOpenChange={o=>{if(!o)setInterview(null)}} title="Schedule interview" description={interview?`Pick when you will interview ${interview.name}. They are notified of the status change.`:undefined} submitLabel="Schedule interview" busy={!!(interview&&updating[interview.id])} submitDisabled={!interview?.date} onSubmit={saveInterview}><div><Label htmlFor="interview-date">Interview date and time</Label><input id="interview-date" type="datetime-local" required min={localNow()} value={interview?.date||''} onChange={e=>setInterview(x=>x&&{...x,date:e.target.value})} className={fieldClass}/></div></FormDialog>
<FormDialog open={!!notes} onOpenChange={o=>{if(!o)setNotes(null)}} title="Rate and note" description={notes?`Internal to your team; ${notes.name} never sees this.`:undefined} submitLabel="Save notes" busy={!!(notes&&updating[notes.id])} onSubmit={saveNotes}><div><Label htmlFor="recruiter-note">Recruiter note</Label><Textarea id="recruiter-note" maxLength={2000} value={notes?.note||''} onChange={e=>setNotes(x=>x&&{...x,note:e.target.value})} className="mt-2 bg-black/20 border-white/15 min-h-28"/></div><div><Label htmlFor="recruiter-rating">Internal rating</Label><select id="recruiter-rating" value={notes?.rating||''} onChange={e=>setNotes(x=>x&&{...x,rating:e.target.value})} className={fieldClass+' bg-slate-900'}><option value="">No rating</option>{[1,2,3,4,5].map(n=><option key={n} value={n}>{n} / 5</option>)}</select></div></FormDialog>
</main></div>}
