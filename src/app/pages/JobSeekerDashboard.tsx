import {useEffect,useState} from 'react';import {Navigation} from '../components/Navigation';import {Card,CardContent} from '../components/ui/card';import {Button} from '../components/ui/button';import {Badge} from '../components/ui/badge';import {Progress} from '../components/ui/progress';import {apiGet} from '../lib/api';import {useAuth} from '../lib/authContext';import {Link} from 'react-router';import {Briefcase,Calendar,Search,User,Bookmark,ArrowRight,ShieldCheck} from 'lucide-react';
export default function JobSeekerDashboard(){const{user}=useAuth();const[d,setD]=useState<any>({recommendedJobs:[]}),[state,setState]=useState<'loading'|'ready'|'error'>('loading');const load=()=>{setState('loading');apiGet<any>('/dashboard').then(x=>{setD({...x,recommendedJobs:x?.recommendedJobs||[]});setState('ready')}).catch(()=>setState('error'))};useEffect(load,[]);const Stat=({n,label,icon:I}:{n:any,label:string,icon:any})=><Card className="bg-white/[.055] border-white/10"><CardContent className="p-5 flex items-center gap-4"><div className="rounded-xl bg-violet-500/10 p-3"><I className="text-violet-300"/></div><div><div className="text-2xl font-bold">{n||0}</div><div className="text-sm text-slate-400">{label}</div></div></CardContent></Card>;return <div className="min-h-screen bg-slate-950 text-white"><Navigation/><main className="max-w-7xl mx-auto px-5 md:px-6 pt-28 pb-16"><div className="flex flex-col md:flex-row justify-between gap-5 mb-7"><div><div className="text-xs uppercase tracking-[.22em] text-violet-300 mb-2">Career workspace</div><h1 className="text-4xl md:text-5xl font-bold">Welcome back, {user?.name?.split(' ')[0]}</h1><p className="text-slate-400 mt-2">Focus on credible opportunities and proof of work, not application volume.</p></div><Link to="/jobseeker/jobs"><Button><Search size={16} className="mr-2"/>Explore opportunities</Button></Link></div>{state==='error'&&<div role="alert" className="mb-6 rounded-xl border border-rose-400/20 bg-rose-500/10 p-4 text-sm flex flex-wrap items-center justify-between gap-3"><span>Your dashboard could not be loaded, so the numbers below may be out of date.</span><Button size="sm" variant="outline" onClick={load}>Retry</Button></div>}<div className="grid md:grid-cols-3 gap-4 mb-7"><Stat n={d.applications} label="Applications" icon={Briefcase}/><Stat n={d.interviews} label="Interviews" icon={Calendar}/><Stat n={d.saved} label="Saved opportunities" icon={Bookmark}/></div><Card className="bg-gradient-to-br from-violet-500/10 to-sky-500/[.06] border-white/10 mb-8"><CardContent className="p-5 md:p-6"><div className="flex flex-col md:flex-row md:items-center justify-between gap-4"><div className="max-w-2xl"><div className="flex items-center gap-2"><h2 className="text-xl font-semibold">Career profile strength</h2>{user?.verified&&<Badge className="bg-emerald-500/15 text-emerald-300"><ShieldCheck size={13} className="mr-1"/>Verified</Badge>}</div><p className="text-sm text-slate-400 mt-1">Credits, portfolio proof, skills, genres and availability are stronger hiring signals than a generic bio.</p></div><div className="w-full md:w-64"><div className="flex justify-between text-sm mb-2"><span>Completeness</span><span>{d.profileScore||0}%</span></div><Progress value={d.profileScore||0}/><Link to="/jobseeker/profile" className="text-xs text-violet-300 mt-2 inline-flex items-center">Improve profile <ArrowRight size={12} className="ml-1"/></Link></div></div></CardContent></Card><div className="flex items-center justify-between mb-4"><div><h2 className="text-2xl font-semibold">Best current fits</h2><p className="text-sm text-slate-500 mt-1">Transparent profile-fit score based on skills, genre, location and work mode—not a black-box AI verdict.</p></div></div>{state==='ready'&&!d.recommendedJobs?.length&&<Card className="bg-white/5 border-white/10"><CardContent className="p-8 text-center text-slate-400">No open opportunities to recommend right now. <Link to="/jobseeker/alerts" className="text-violet-300">Set up an alert</Link> to hear about new ones.</CardContent></Card>}<div className="grid md:grid-cols-2 xl:grid-cols-3 gap-4">{d.recommendedJobs?.map((j:any)=><Link key={j.id} to={`/jobseeker/jobs/${j.id}`}><Card className="h-full bg-white/[.055] border-white/10 hover:bg-white/[.075]"><CardContent className="p-5"><div className="flex justify-between gap-3"><Badge variant="secondary">{j.opportunity_kind}</Badge><Badge className="bg-sky-500/15 text-sky-200">{j.fitScore}% fit</Badge></div><h3 className="font-semibold text-lg mt-4">{j.title}</h3><p className="text-violet-300 text-sm mt-1">{j.company}</p><p className="text-sm text-slate-400 mt-3">{j.location} · {j.workplace}</p></CardContent></Card></Link>)}</div></main></div>}
