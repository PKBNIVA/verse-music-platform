import {useEffect,useState} from 'react';import {Navigation} from '../components/Navigation';import {apiDelete,apiGet,apiPost} from '../lib/api';import {Card,CardContent} from '../components/ui/card';import {Button} from '../components/ui/button';import {Input} from '../components/ui/input';import {Badge} from '../components/ui/badge';import {Building2,Plus,UserPlus,Trash2} from 'lucide-react';import {toast} from 'sonner';
export default function Workspace(){const[orgs,setOrgs]=useState<any[]>([]),[selected,setSelected]=useState<any>(null),[members,setMembers]=useState<any[]>([]),[name,setName]=useState(''),[invite,setInvite]=useState({email:'',role:'recruiter'});const load=()=>apiGet<any>('/organizations').then(d=>{setOrgs(d.organizations||[]);if(!selected&&d.organizations?.[0])setSelected(d.organizations[0])});useEffect(()=>{load()},[]);useEffect(()=>{if(selected)apiGet<any>(`/organizations/${selected.id}/members`).then(d=>setMembers(d.members||[]))},[selected?.id]);async function create(){try{await apiPost('/organizations',{name});setName('');await load();toast.success('Workspace created')}catch(e:any){toast.error(e.message)}}async function add(){try{await apiPost(`/organizations/${selected.id}/members`,invite);setInvite({...invite,email:''});const d:any=await apiGet(`/organizations/${selected.id}/members`);setMembers(d.members);toast.success('Team member added')}catch(e:any){toast.error(e.message)}}async function remove(id:string){try{await apiDelete(`/organizations/${selected.id}/members/${id}`);setMembers(members.filter(m=>m.id!==id))}catch(e:any){toast.error(e.message)}}return <div className="min-h-screen bg-slate-950 text-white"><Navigation/><main className="max-w-6xl mx-auto px-5 pt-28 pb-16"><div className="mb-7"><div className="text-xs uppercase tracking-[.2em] text-violet-300">Team SaaS workspace</div><h1 className="text-4xl font-bold mt-2">Workspace & seats</h1><p className="text-slate-400 mt-2">Bring recruiters, bookers and finance into the same operating account. Seat limits are enforced by the workspace owner's plan.</p></div><div className="grid lg:grid-cols-[.7fr_1.3fr] gap-6"><div className="space-y-4"><Card className="bg-white/[.055] border-white/10"><CardContent className="p-5"><h2 className="font-semibold flex gap-2 items-center"><Building2 size={17}/>Create workspace</h2><div className="flex gap-2 mt-4"><Input placeholder="Studio / label / band / agency" value={name} onChange={e=>setName(e.target.value)}/><Button size="icon" onClick={create} disabled={!name.trim()}><Plus size={16}/></Button></div></CardContent></Card>{orgs.map(o=><button key={o.id} onClick={()=>setSelected(o)} className={`w-full text-left rounded-xl border p-4 ${selected?.id===o.id?'border-violet-400 bg-violet-500/10':'border-white/10 bg-white/[.035]'}`}><div className="font-semibold">{o.name}</div><div className="text-xs text-slate-500 mt-1">{o.memberCount} member{o.memberCount===1?'':'s'} · you are {o.memberRole}</div></button>)}</div><Card className="bg-white/[.055] border-white/10"><CardContent className="p-6">{selected?<><div className="flex justify-between items-start gap-3"><div><h2 className="text-xl font-semibold">{selected.name}</h2><p className="text-sm text-slate-400">Team access is role-scoped and seat-limited.</p></div><Badge>{selected.memberRole}</Badge></div>{['owner','admin'].includes(selected.memberRole)&&<div className="grid md:grid-cols-[1fr_150px_auto] gap-2 mt-5"><Input placeholder="Existing Verse user email" value={invite.email} onChange={e=>setInvite({...invite,email:e.target.value})}/><select className="h-10 rounded-md bg-slate-900 border border-white/10 px-3" value={invite.role} onChange={e=>setInvite({...invite,role:e.target.value})}>{['admin','recruiter','booker','finance','member'].map(x=><option key={x}>{x}</option>)}</select><Button onClick={add} disabled={!invite.email}><UserPlus size={16}/></Button></div>}<div className="space-y-3 mt-6">{members.map(m=><div key={m.id} className="flex justify-between gap-3 items-center border-b border-white/10 pb-3"><div><div className="font-medium">{m.name}</div><div className="text-xs text-slate-500">{m.email}</div></div><div className="flex items-center gap-2"><Badge variant="secondary">{m.role}</Badge>{m.role!=='owner'&&['owner','admin'].includes(selected.memberRole)&&<Button variant="ghost" size="icon" onClick={()=>remove(m.id)}><Trash2 size={15}/></Button>}</div></div>)}</div></>:<div className="text-slate-500 text-center py-16">Create or select a workspace.</div>}</CardContent></Card></div></main></div>}
